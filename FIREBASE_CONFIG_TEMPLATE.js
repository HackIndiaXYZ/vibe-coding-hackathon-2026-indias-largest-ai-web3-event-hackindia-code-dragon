/**
 * Firebase Config Template
 * 
 * Instructions:
 * 1. Go to Firebase Console → Ren-AI project → Settings ⚙️
 * 2. Copy your web app config object
 * 3. Paste it below in place of the placeholder values
 * 4. Save this file or paste directly into index.html before the Firebase loader script
 */

// Add this to index.html in a <script> tag BEFORE the Firebase loader:
/*
<script>
  window.FIREBASE_CONFIG = {
    apiKey: "YOUR_API_KEY_HERE",
    authDomain: "your-project.firebaseapp.com",
    projectId: "your-project-id",
    storageBucket: "your-project.appspot.com",
    messagingSenderId: "YOUR_SENDER_ID",
    appId: "1:YOUR_APP_ID:web:YOUR_WEB_ID"
  };
</script>
*/

// Alternative: If using environment variables in your build system:
// window.FIREBASE_CONFIG = JSON.parse(import.meta.env.VITE_FIREBASE_CONFIG || '{}');
